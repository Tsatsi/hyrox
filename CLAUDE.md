# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

A Node.js (CommonJS) application for HYROX race preparation planning. The project is in its initial state — only a `package.json` exists.

## Commands

```bash
# Install dependencies
npm install

# Run the app (once index.js exists)
node index.js

# Run tests (once configured)
npm test
```
